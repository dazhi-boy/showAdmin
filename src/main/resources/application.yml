spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    initialization-mode: always
    schema: classpath:sql/schema.sql
    data: classpath:sql/data.sql
    url: jdbc:mysql://localhost:3306/showgoods?characterEncoding=UTF8&useSSL=false&allowMultiQueries=true&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
    platform: mysql
